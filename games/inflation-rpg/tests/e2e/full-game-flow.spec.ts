// Replaced by full-run.spec.ts — this file kept as a placeholder to avoid empty e2e directory issues.
// The old Phaser-scene-based tests were removed when the architecture migrated to React+Phaser.
import { test, expect } from '@playwright/test';

test('placeholder — see full-run.spec.ts for active E2E tests', async () => {
  expect(true).toBe(true);
});
