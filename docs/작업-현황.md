# 현재 상태

- 기록일: 2026-09-12.
- 제품: **신의 마을: 영원의 후원자**. 출시 전 현재 게임이며 자율 개발은 기본
  중단이다. 실행 여부는 이 문서와 제어기 상태로 판단한다.
- Android 우선: `com.shinui.eternalsponsor`.
- 웹은 같은 도메인 로직·입력 흐름·레이아웃을 검증하는 개발 표면이다.
- canonical 저장 키: `shin-ui-eternal-sponsor-save-v2`.
- 외부 사용자 저장 호환 계약은 없다. 현재 저장 schema 2의 형태만 읽는다.
- push·예약 활성화·완료 후 자동 반복은 수행하지 않는다.
- 사용자 `output/`, `tmp/`와 보호된 Paradox 파일은 변경·커밋하지 않는다.

## 이번 정리

현재 게임의 단일 부팅 경로와 package·route·Android 식별자를 정리하고, 사용하지
않는 구형 실행 트리·도구·에셋·의존성을 제거했다. 저장 boundary는 canonical
key 하나와 현재 schema 2 형태만 사용하며, 개발 중 이전 저장 형태를 자동
보정하지 않는다. 포털은 현재 게임 하나만 등록한다.

Village domain 구현은 `village/domain.ts`의 47줄 compatibility façade와
hero·facility·expedition·story·intervention·rewards·settings direct module로
분리했다. 의미가 드러나는 shared helper만 두고 production domain module의
façade 역참조를 금지했다. 저장은 계속 `village/save.ts` 경계, canonical key
`shin-ui-eternal-sponsor-save-v2`, schema 2를 사용한다. RPG 고유 계약의
`@forge/core` 승격은 별도 두 번째 게임에서 재사용 요구가 확인될 때까지 보류한다.

## 검증 기록

2026-09-12 domain 분해 로컬 검증(기준 `4fd30c10`, 구현 HEAD `dfa32ed4`):

- façade 47줄. 구현 선언 검색은 일치 없음(`rg` 종료 1).
- 게임 package typecheck 종료 0. unit test 233개 파일·2,093개 테스트 통과.
- `pnpm e2e` 종료 0: 포털 4개, 게임 32개(Chromium 16·iPhone 14 16), 합계 36개 통과.
- `pnpm build` 종료 0: build task 3/3 성공, 1개 cached. 게임 정적 페이지 4/4,
  dev-shell 3/3 생성.
- `pnpm typecheck` 5/5(3 cached), `pnpm lint` 4/4(2 cached), 모두 종료 0.
- 직접 Madge는 522개 파일·경고 5건, build 뒤 `pnpm circular`은 527개 파일·
  경고 4건을 검사했고 모두 순환 의존성 없음(종료 0).
- `git diff --check 4fd30c10..HEAD` 종료 0.
- active tree의 금지된 역사 자료 경로에서 추적 계획·설계 문서 두 개를 제거한
  뒤 제품 정체성 검사 7/7 통과(종료 0). 삭제한 내용은 Git 이력에 남는다.
- 보호된 Paradox 파일 세 개는 기준과 현재 hash가 같고 기준 diff·현재 status가
  비어 있다. 사용자 `output/`, `tmp/`는 기준 tree와 현재 filesystem에 없다.

domain 구조·회귀·build·제품 정체성 검증은 모두 통과했다. 실기기·스토어 검증은
아래 범위에 남긴다.

## 아직 측정하지 않은 범위

- 첫 15/30분 수치는 실제 신규 사용자 표본이 없어 측정 전이다.
- Playwright 모바일 profile은 실기기가 아니다. Android 조작·백그라운드 복귀·
  광고·결제 취소/복원/환불은 장비와 스토어 계정으로 확인해야 한다.
- D1/D7, 서버 계정, 클라우드, PvP는 이번 로컬 우선 제품 범위 밖이다.

## 다음 재개 기준

사용자가 다시 시작을 명시하면 먼저 현재 Git 상태와 검증 결과를 확인한다.
실기기 출시 검증을 콘텐츠 추가보다 먼저 선택한다.
