# 현재 상태

- 기록일: 2026-09-08. 기준 코드: main `9872b56b`.
- **출시 준비 보강 작업 실행 중.** 사용자가 승인한 6개 작업과 커밋까지만 진행한다.
- push·merge·예약 활성화·완료 후 자동 반복은 승인되지 않았다.
- 사용자 `output/`, `tmp/`는 변경·커밋하지 않는다.

## 구현과 검증

V4 기본 진입점, V3 legacy, 전용 저장, 시설·에이전트·원정·오프라인·수익화 adapter가 존재한다. 구현 존재는 출시 합격이 아니다.

기준 SHA에서 Vitest 400개 파일/3,638개, game E2E 46/46, V4 32/32, workspace typecheck/lint/circular와 game build를 2026-09-08에 다시 통과시켰다. 이후 변경은 작업별 검증과 최종 전체 게이트를 다시 실행한다. iPhone14 프로필은 실기기 검증이 아니다.

감사에서 실제로 동작하지 않는 정책 자동 행동, 화면과 다른 원정 승률, 빈약한 서사, 첫 30분 계측 부재, 게임 build/E2E가 빠진 CI를 출시 차단 문제로 확인했다. [현재 계획](superpowers/plans/2026-09-06-shin-ui-v4-implementation.md)에 수정 순서와 테스트를 고정했다.

## 이번 검증 범위

기준 SHA `9872b56b` 위에서 로컬 제어기와 회귀 테스트, 문서 기준선을 정리했다. `pnpm test:autonomy`는 기본 중단·승인·단일 lease·이전 실행 거부·현재 작업 종료·권한·작업 한도·무진전 차단·손상 상태·명령과 후손 취소를 검증한다. CI에는 이 테스트 실행 단계를 추가했다. 원격 CI 통과는 최종 후보 SHA에서 별도 확인한다.

루나의 서사·게임플레이·문서/CI 읽기 전용 감사 결과를 현재 계획에 반영했다. 로컬 실행 제어 테스트 16개는 앞선 구축 단계에서 통과했다. 실제 계정 사용량·앱 예약·루나 장기 개발·원격 중단/재개 E2E는 이번 범위가 아니다.

현재 작업 완료 시 제어 상태를 종료하고 자동 재개하지 않는다. 이후 명시적 시작 요청에서 승인·범위·예산·실제 Git 상태를 다시 확인한다.

[이전 상세 현황](archive/2026-09-08-work-status.md)은 역사 기록이다.
