# 현재 상태

- 기록일: 2026-09-09.
- 제품: **신의 마을: 영원의 후원자**. 자율 개발은 기본 중단이며, 실행 여부는 이 문서와 제어기 상태로 판단한다.
- 기준 main: `6be98aad`.
- 기준 main CI run `34317655778`: 성공.
- 기준 main Pages run `34317655047`: 성공.
- 기준 main 게임 테스트 baseline: **402개 파일·3,703개 테스트**.
- 사용자 `output/`, `tmp/`와 보호된 Paradox 파일은 변경·커밋하지 않는다.

## 구현과 검증

현재 게임의 제품 언어·namespace·저장 호환 정리를 완료했다. 신의 마을: 옛 모험의 실행 경로와 저장은 호환 표면으로 격리했고, 현재 화면에는 세대명·이전 테마명이 노출되지 않는다. 신목 들판(`sacred_fields`)을 첫 영역으로 사용하며, 이전 저장 가져오기는 명시적 선택으로만 실행한다.

이번 정리 브랜치의 최신 검증은 `pnpm test:autonomy` 32/32 통과(기존 현황 기록 26/26에서 start authorization 회귀 6개 추가), 게임 테스트 403개 파일·3,713개 테스트, 게임 typecheck 통과, canonical 문서 13개 링크·경로 점검 통과, `git diff --check` 통과다. 32/32는 현재 브랜치의 자율·정체성·CI·runner 테스트 결과이며, 기준 main의 402개 파일·3,703개 게임 테스트 baseline과 구분된다. 실행 상태와 선택적 task budget은 이 문서와 `node scripts/autonomy/control.mjs status`의 현재 출력으로 판단한다.

## 아직 측정하지 않은 범위

- 첫 15/30분 수치는 실제 신규 사용자 표본이 없어 측정 전이다.
- iPhone 14 Playwright 프로필은 실기기가 아니다. iOS/Android 복귀·광고·결제 취소/복원/환불은 장비와 스토어 계정으로 확인해야 한다.
- D1/D7, 서버 계정, 클라우드, PvP는 이번 로컬 우선 제품 범위 밖이다.

## 다음 재개 기준

사용자가 다시 시작을 명시하면 먼저 현재 Git 상태와 원격 CI 여부를 확인한다. 새 generation과 task 예산을 만든 뒤 `QA-01` 또는 `DEVICE-01`을 우선한다. 콘텐츠 추가는 출시 게이트를 통과한 다음 선택한다.
