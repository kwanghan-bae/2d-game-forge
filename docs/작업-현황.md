# 현재 상태

- 기록일: 2026-09-11. 기준 branch: `main`; V4 진입점 분리와 과거 `STATUS-*` 기록 보관 이동은 커밋 완료 상태다.
- **출시 후보 기술 보강 6개 작업 로컬 완료.** 최종 CI·문서 커밋 뒤 제어 상태는 task 한도에 따라 종료한다.
- push·merge·예약 활성화·완료 후 자동 반복은 수행하지 않는다.
- 사용자 `output/`, `tmp/`는 변경·커밋하지 않는다.

## 구현과 검증

V4 기본 진입점, V3 legacy, 전용 저장, 시설·지원 인물·원정·오프라인·수익화 adapter 위에 다음 출시 차단 보강을 완료했다.

- 정책별 15초 영웅 자율 판단과 저HP·미확인 결과 안전 경계
- 화면 예측과 실제 정산이 공유하는 원정 forecast
- 프롤로그→세 영역 승리→깊은 숲 선택→저승 에필로그의 최소 서사 호
- 신뢰 50 관계 기록, 회춘·영역 진입·승리 사가 기록
- 개인정보 없는 최대 500건 로컬 계측과 첫 15/30분 진단
- 한국어 화면 용어, 조선 무사 픽셀 프레임, 760px 이상 마을 2열 레이아웃
- 게임 build·게임 E2E·포털 E2E를 분리한 CI 게이트와 placeholder 제거

최종 로컬 검증은 자율 제어 16/16, 게임 Vitest 402개 파일·3,703개 테스트, V4 452/452, 게임 E2E 44/44, game build, workspace typecheck/lint/circular, `git diff --check`를 통과했다. 390×844는 문서 폭과 viewport 폭이 모두 390px였고, 데스크톱은 영웅/목표와 시설/지원 인물이 각각 2열로 계산됐다.

## 아직 출시 완료가 아닌 이유

- 최종 후보 SHA는 아직 push하지 않았으므로 GitHub Actions 결과가 없다.
- 첫 15/30분 수치는 계산 가능해졌지만 실제 신규 사용자 표본이 없다.
- iPhone 14 Playwright 프로필은 실기기가 아니다. iOS/Android 복귀·광고·결제 취소/복원/환불은 장비와 스토어 계정으로 확인해야 한다.
- D1/D7, 서버 계정, 클라우드, PvP는 이번 로컬 우선 제품 범위 밖이다.

## 다음 재개 기준

사용자가 다시 시작을 명시하면 먼저 현재 Git 상태와 원격 CI 여부를 확인한다. 새 generation과 task 예산을 만든 뒤 `QA-01` 또는 `DEVICE-01`을 우선한다. 콘텐츠 추가는 이 출시 게이트를 통과한 다음 선택한다.

[이전 상세 현황](archive/2026-09-08-work-status.md)은 역사 기록이다.
