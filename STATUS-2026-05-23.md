# STATUS — 2026-05-23 (V3-A + V3-B 머지 직후)

이 파일은 다음 세션의 진입점.

## 한 줄

V3 (Eternal Hero, Idle Sponsor) 의 첫 두 phase 가 main 에 머지됨. 다음 = **V3-C (빛 spend modal + 7 buff catalog, 4-6h)**.

## 오늘 세션 산출 (2026-05-23)

| 단계 | commit / tag | 비고 |
|---|---|---|
| V2 → V3 spec 작성 | `a7bec1a` + amendment `4343670` | `docs/superpowers/specs/2026-05-23-v3-eternal-hero-idle-sponsor-design.md` (395+ lines) |
| Phase V3-A plan | `48553aa` | `docs/superpowers/plans/2026-05-23-phase-v3-a-movement-polish.md` (7 task) |
| Phase V3-A 머지 | `b016d59`, tag `phase-v3-a-complete` | 9 files / +177/-7. Pathfinder bounds + 변동 idle pause + chapter_transition overlay |
| Phase V3-B plan | `99aeae9` | `docs/superpowers/plans/2026-05-23-phase-v3-b-eternal-hero.md` (9 task) |
| Phase V3-B 머지 | `4002f55`, tag `phase-v3-b-complete` | 38 files / +553/-238. BP 폐기 + action-time aging + 회춘 + 디버프 + persist v19 clean reset |

총 commits ahead of origin/main: 32.

## V3-B 핵심 결정 (user-confirmed, 이미 코드에 baked in)

1. **BP 완전 폐기.** HeroEntity 의 bp/bpMax/dead/consumeBp/refreshAge 다 제거.
2. **회춘 cost = (age - 5) × 10 빛** (linear, age<=5 → 0). `games/inflation-rpg/src/hero/rejuvenation.ts` 참조.
3. **Persist v18→v19 = clean reset.** sponsorGold/atkBaseBonus/hpBaseBonus/cycleHistory 모두 drop, meta.light = 0 부터 시작.

## 다음 phase = V3-C (빛 spend modal + buff catalog)

V3 spec §9 V3-C, 4-6h estimate.

목표:
- **빛 자원 누적 wire-up** — 현재 meta.light 는 hero action 누적과 연동 안 됨. CycleControllerV2.handleArrival 안에서 적 처치/drop/level up 마다 light 누적 (spec §5.1 magnitude).
- **7 buff catalog** (spec §5.2) — 이동속도/장비획득률/빛속도/회춘 cost 할인/aging 속도/필드 디버프 한도 + 일회용 회춘.
- **Spend modal UI** — Main 화면 "신의 메뉴" 버튼 → 모달. buff 별 카드 (현재 Lv / 다음 효과 / cost / ×N 한꺼번에 사기). HUD 의 임시 회춘 5년 버튼은 정식 modal 로 대체.

이미 와이어드 (V3-B 산출물):
- `meta.light: number` (types.ts)
- `cycleSliceV2.rejuvenateHero(years)` 패턴
- `rejuvenationCost(age)` helper
- `getAgingDebuff(age).moveMul` (현재 unused, V3-D 에서 zone scene 이 활용 예정)

## 다음 세션 진입 절차

```bash
git log --oneline -5
git tag --list | grep v3
git status --short
```

그 다음 `/goal` 또는 직접 "V3-C 시작" 으로 brainstorming → writing-plans → subagent-driven-development 체인.

## 미해결 / 보류

- **origin push**: 32 commits ahead. user 가 push 시점 결정. main → origin push 는 user 명시 후만.
- **V3 spec §4.5 personality combat**: V3-E 이후로 미룸. V3-B 까지는 raw "강한적 = 패배 가능" 만.
- **`getAgingDebuff(age).moveMul`**: 코드는 있지만 소비처 없음. V3-D zone scene 에서 hero 이동 tween duration 곱하면 활성화.
- **V3 saga book UI**: V3-F scope. 현재 saga 는 record 되지만 무한 chapter viewer 없음.

## 알려진 트랩

- V3-A의 chapter_transition push 는 tickAge 후에 위치 — V3-A spec 의 BEFORE 순서는 V3-B 에서 자가모순 (consumeBp 가 사라져서 tickAge 만이 age 변경). 코드는 올바른 순서로 commit `88d09bd`.
- `cycleHistory` 등 v14-v17 의 intermediate state 검증 테스트 4건은 V3-B 에서 삭제됨 (`8af7efd`) — v19 clean reset 이 wipe 하므로 의미 상실. 새 migration 추가 시 같은 패턴으로 obsolete 처리.
