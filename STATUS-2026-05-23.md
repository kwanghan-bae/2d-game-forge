# STATUS — 2026-05-23 (V3-A + V3-B + V3-C 머지 직후)

이 파일은 다음 세션의 진입점.

## 한 줄

V3 (Eternal Hero, Idle Sponsor) 의 세 phase (A 이동 polish + B 영원 hero + C 신의 빛 / buff catalog / spend modal) 가 main 에 머지됨. 다음 = **V3-D (Multi-zone, 4-6h)** 또는 telemetry 기반 곡선 측정.

## 오늘 세션 산출 (2026-05-23)

| 단계 | commit / tag | 비고 |
|---|---|---|
| V2 → V3 spec 작성 | `a7bec1a` + amendment `4343670` | `docs/superpowers/specs/2026-05-23-v3-eternal-hero-idle-sponsor-design.md` (414 lines) |
| Phase V3-A plan | `48553aa` | `docs/superpowers/plans/2026-05-23-phase-v3-a-movement-polish.md` (7 task) |
| Phase V3-A 머지 | `b016d59`, tag `phase-v3-a-complete` | 9 files / +177/-7. Pathfinder bounds + 변동 idle pause + chapter_transition overlay |
| Phase V3-B plan | `99aeae9` | `docs/superpowers/plans/2026-05-23-phase-v3-b-eternal-hero.md` (9 task) |
| Phase V3-B 머지 | `4002f55`, tag `phase-v3-b-complete` | 38 files / +553/-238. BP 폐기 + action-time aging + 회춘 + 디버프 + persist v19 clean reset |
| Phase V3-C sub-spec | `5a232b6` | `docs/superpowers/specs/2026-05-23-phase-v3-c-light-buff-design.md` (453 lines) |
| Phase V3-C plan | `1e94c68` | `docs/superpowers/plans/2026-05-23-phase-v3-c-light-buff.md` (16 task, 1891 lines) |
| Phase V3-C 머지 | `4f63c2d`, tag `phase-v3-c-complete` | 21 files / +955/-27. 7 buff catalog + spend modal + light emit + selector pattern + persist v20 |

총 commits ahead of origin/main: 48.

## V3-C 핵심 결정 (user-confirmed, 이미 코드에 baked in)

1. **Scope = Spec §5 그대로 7 buff** (move_speed/drop_chance/light_rate/rejuv_discount/aging_slow/field_diff/oneshot_rejuv).
2. **HUD 회춘 5년 버튼 제거** + "신의 메뉴" 버튼 단일 입구.
3. **Light HUD = 카운터 + floating "+N"** debounced.
4. **Quick buy = ×1 / ×10 / ×Max** 고정.
5. **Buff #6 (필드 디버프 한도)** = catalog 포함, V3-D 까지 inert. 카드에 "V3-D 도착 시 활성" 표시.
6. **Balance baseline** = spec §5.1 / §5.2 값 그대로 placeholder. V3-G 에서 pass.
7. **Cost cap** = `rejuv_discount` 0.80 / `aging_slow` 0.50 (master spec 에 없는 V3-C 추가 결정, V3-G 재검토).
8. **Encounter positive** = `shrine_visited` + `skill_learned` + `job_unlocked` 3종.
9. **Modal 게임 멈추지 않음** (idle 의미 유지).
10. **Controller pure 유지** — light emit 은 OverworldRunner 가 events 로 처리. Headless sim 영향 없음.
11. **Selector pattern** — `buff/buffEffects.ts` 의 6 selector 로 모든 buff 효과 통일.

## 다음 phase = V3-D (Multi-zone, 4-6h)

V3 spec §9 V3-D, 4-6h estimate.

목표:
- **Zone catalog 확장** — base map + 5 realm = 6 zone (현재는 base map 만).
- **Field level system** — zone 별 field level. hero level vs field level diff 가 stat damping (이동속도/데미지).
- **Buff #6 활성화** — `getFieldDiffThreshold(meta)` 가 damping 의 threshold 로 consume.
- **Hero zone navigation AI** — HeroDecisionAI 가 zone 진행 결정.
- **Zone clear 조건** — zone boss 처치 → 다음 zone unlock.

이미 와이어드 (V3-C 산출물):
- `meta.buffLevels.field_diff` (catalog 포함 + Lv 누적 가능)
- `getFieldDiffThreshold(meta)` selector (consume 자리만 비어있음)
- `getMoveSpeedMul`, `getDropChanceBonus`, `getLightRateMul`, `getRejuvDiscount`, `getAgingSpeedMul` 모두 wire 완료

## 다음 세션 진입 절차

```bash
git log --oneline -5
git tag --list | grep v3
git status --short
```

그 다음 `/goal` 또는 직접 "V3-D 시작" 으로 brainstorming → writing-plans → subagent-driven-development 체인.

## 미해결 / 보류 (V3-C 이후)

- **origin push**: 48 commits ahead. user 가 push 시점 결정. main → origin push 는 user 명시 후만.
- **V3 spec §4.5 personality combat**: V3-E 이후로 미룸. V3-B 까지는 raw "강한적 = 패배 가능" 만.
- **V3 saga book UI**: V3-F scope. 현재 saga 는 record 되지만 무한 chapter viewer 없음.

## Pre-existing 부채 (V3-C 와 무관, 별도 추적)

V3-C 검증 단계에서 발견됨 — V3-C 가 도입한 회귀 아님:

1. **`v2-vertical-slice.spec.ts` e2e 600s timeout** — V3-B 가 BP cycle 자연 종료 폐기 → 이 spec 의 `cycle-result-v2` testid 가 hit 안 됨. V3-B 머지 시점부터 깨져있었음. STATUS-2026-05-23 의 V3-B "complete" 시점에 보고되지 않음. 별도 fix 권장 (test.fixme 또는 cycle-end forced path).
2. **circular check 실패** — `HeroEntity ↔ JobSystem` 순환. base commit `3f9cc9e` 부터 존재. CLAUDE.md 의 `pnpm circular` 가 CI 강제이지만 실제로는 빨간 상태. 별도 cleanup task.

## 알려진 트랩

- V3-A의 chapter_transition push 는 tickAge 후에 위치 — V3-A spec 의 BEFORE 순서는 V3-B 에서 자가모순 (consumeBp 가 사라져서 tickAge 만이 age 변경). 코드는 올바른 순서로 commit `88d09bd`.
- `cycleHistory` 등 v14-v17 의 intermediate state 검증 테스트 4건은 V3-B 에서 삭제됨 (`8af7efd`) — v19 clean reset 이 wipe 하므로 의미 상실. 새 migration 추가 시 같은 패턴으로 obsolete 처리.
- **V3-C `MetaState.buffLevels: Partial<Record<BuffId, number>>` 은 required (not optional)** — 모든 MetaState literal 이 `buffLevels: {}` 를 포함해야 typecheck 통과. V3-C T1 implementer 가 이 사실로 plan 의 "DO NOT modify INITIAL_META" 를 deviated. 이 패턴 = 필요 시 합리적 deviation.
- **Persist 마이그레이션 chain v19→v20** — `migrateV19ToV20` 가 buffLevels 빈 객체 주입. 새 buff 추가 시 catalog 만 수정하면 됨 (Lv 0 = 효과 없음 = selector baseline). 새 migration 필요 없음.

## 검증 결과 요약 (머지 직전 기준)

- vitest: 983/983 (96 files)
- typecheck: 5/5 packages 0 exit
- lint: 4/4 packages 0 exit
- E2E (V3-C scope): `v3-c-spend-modal` (chromium + iphone14), `v9-migration` (v20 fixup 후) 모두 통과
- E2E (regression): `full-game-flow` 통과
- E2E (pre-existing 부채): `v2-vertical-slice` fail (V3-B fallout, 위 참조)
- circular: fail (base 부터 pre-existing)
